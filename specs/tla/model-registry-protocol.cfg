\* FIXED model configuration — verifying all invariants pass
\*
\* This models the FIXED registry protocol with:
\*   Bug 1 fix: Pre-increment refcount in Acquire (AcquirePreIncRef before Get)
\*   Bug 2 fix: Load lock in all registries (no HasLoadLock toggle, always locked)
\*   Bug 4 fix: Orphan tracking instead of re-add in eviction callback
\*
\* Result: ALL invariants PASS
\*   18,101,627 states generated, 3,788,089 distinct, depth 46
\*   TypeInvariant      — PASSES
\*   NoUseAfterClose    — PASSES (Bug 1 fix verified)
\*   NoResourceLeak     — PASSES (Bug 2+4 fix verified)
\*   NoDoubleClose      — PASSES
\*   RefCountConsistency — PASSES
\*
\* Previous buggy model results for comparison:
\*   HasLoadLock=FALSE: NoUseAfterClose VIOLATED (5 steps), NoResourceLeak VIOLATED (8 steps)
\*   HasLoadLock=TRUE:  NoUseAfterClose VIOLATED (6 steps), NoResourceLeak VIOLATED (10 steps)

SPECIFICATION Spec

CONSTANTS
    Models = {m1, m2}
    Handlers = {h1, h2}
    MaxHandleId = 6

INVARIANTS
    TypeInvariant
    NoUseAfterClose
    NoResourceLeak
    NoDoubleClose
    RefCountConsistency

CHECK_DEADLOCK FALSE
